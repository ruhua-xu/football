当前架构修订确认通过，不再进行总体架构调整。

在进入 MVP 实现前补充一个竞彩规则约束：

PayoutPolicy / BettingRules 增加可配置参数：

```yaml
sporttery:
  base_stake_fen: 200
  max_multiplier: 50
  max_ticket_stake_fen: 600000
```

程序允许基础 1 倍投注，因此：

```text
1 <= multiplier <= max_multiplier
```

总票面金额必须满足：

```text
stake_fen <= max_ticket_stake_fen
```

这些参数不得散落写死在 Optimizer 中，由版本化竞彩规则配置统一管理。

现有奖金舍入规则保持不变，继续采用当前已设计的官方 half-even 规则。

完成这个小修订后，正式进入 MVP 工程实现。

请按照已经确认的架构和开发顺序开始实现，不再重新设计总体架构。

第一阶段目标：

1. 建立实际 Python 工程骨架。
2. 实现 Domain Model。
3. 实现 SQLite / SQLAlchemy / Alembic。
4. 实现 MockFixtureProvider、MockMarketOddsProvider、MockSportteryProvider。
5. 准备 5～10 场固定 Mock 比赛。
6. 实现 THREE_WAY 的 P_market 去水。
7. 实现手工 P_quant。
8. 实现 QUANT_ONLY_V1 和 MARKET_QUANT_BLEND_V1。
9. 实现 Selection EV。
10. 实现简单 2串1 TicketCandidate。
11. 实现竞彩奖金计算与官方舍入。
12. 实现 Portfolio Optimizer。
13. 支持 100元、200元和自定义预算。
14. 最多输出4张顶层 Ticket。
15. 支持 NO_BET。
16. 实现 CLI。
17. 实现不可变 AnalysisRun。
18. 完成 unit / integration / contract / e2e pytest。

每完成一个模块立即运行测试。

禁止为了赶进度跳过测试。

暂时不要实现：

真实网站/API
真实 GPT
analysis_packet/llm_review 文件桥
3串4
4串11
复式
Web 前端
自动下注

MVP 完成后的验收必须提供：

* pytest 完整结果
* CLI 演示
* 一次完整 AnalysisRun
* 5～10 场 Mock 输入
* P_market / P_quant / P_final 输出
* Selection EV
* 生成的 2串1候选
* 100元 Portfolio
* 200元 Portfolio
* NO_BET 示例
* SQLite 中保存的数据示例

完成 MVP 后停止继续扩展，并生成 `mvp_implementation_report.md` 供架构复核。
